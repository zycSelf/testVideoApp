import { FileData } from '@/common/utils/ffmpeg/src/types';

export function screenShot(fileData: FileData, time: number) {
  const video = document.createElement('video');
  const canvas = document.createElement('canvas');
}
